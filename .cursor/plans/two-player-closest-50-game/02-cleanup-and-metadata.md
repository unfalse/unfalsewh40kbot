# Subtask 02: Cleanup unused demo and metadata

## Plan links

- [Plan overview](./00-overview.md)
- Depends on: [01 — Implement round game UI and logic](./01-implement-round-game.md)
- Next: none

## Objective

Remove dead code from the old home demo and align basic app metadata with the new screen.

## Context

- After [01](./01-implement-round-game.md), `src/app/_components/click-counter.tsx` should be unused. Confirm with search; delete the file if unused.
- `src/app/layout.tsx` metadata title/description may still say “T3 App” / create-t3-app blurb.

## Steps

1. Delete `src/app/_components/click-counter.tsx` if no imports remain.
2. Update `layout.tsx` `metadata` title (and description if desired) to reflect the game (neutral English is fine).
3. Run `npm run build` in `test-orchestration/t3-stack-project` and fix any fallout.

## Acceptance criteria

- [ ] No orphaned `click-counter` file or imports.
- [ ] Build succeeds; metadata no longer claims only generic “Generated by create-t3-app” unless you intentionally keep description minimal.

## Notes / risks

- If something still imports `click-counter`, fix imports instead of deleting until clean.
